dist: xenial
language: cpp

addons:
  apt:
    packages:
      - ninja-build

matrix:
  include:
    - compiler: gcc
    - compiler: clang
      env:
        - EXTRA_FLAGS="-stdlib=libc++"

script: ./scripts/build -c auto

